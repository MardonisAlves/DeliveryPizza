<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="description" content="{{ page.description }}">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{ page.title }}</title>

        {# Google fonts:Open Sans #}
    <link rel="stylesheet" type="text/css" href="public/css/materialize.css">
    <link rel="stylesheet" type="text/css" href="public/css/front-style-materialize.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <script src="public/js/jquery.min.js"></script>
    <!--<script src="public/js/bootstrap-4.1.min.js"></script>-->
    <script src="public/js/bootstrap.min.js"></script>
    <!--<script src="public/js/fontawesome.js"></script>-->
    <script src="public/js/materialize.js"></script>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script src="public/js/piecharts.js"></script>
    <script src="public/js/barcharts.js"></script>
    <script src="https://canvasjs.com/assets/script/canvasjs.min.js"></script>
    </head>
    <body>

        <div class="wrapper">
            {% include 'partials/editproduto.twig' %}

            {% include 'partials/editarcardapio.twig'%}
            
            {% include 'partials/navAdmin.twig' %}

            {% block content %}
               
            {% endblock %}
        

        </div>
       

    </body>


    <script type="text/javascript">
        $(document).ready(function () {

            $('#sidebarCollapse').on('click', function () {
                $('#sidebar').toggleClass('active');
            });
        });

         /* select new user*/
        $('select').formSelect();

        /*dropdown*/
        $(".dropdown-trigger").dropdown();

        /*navAdmin*/
        $('.sidenav').sidenav();




    $('#editproduto').on('show.bs.modal', function (event) {

          var button = $(event.relatedTarget)
          var iduser = button.data('iduser')
          var name = button.data('name')
          var qt = button.data('qt');
          var valorCompra = button.data('compra')
          var lucro = button.data('lucro')

          var modal = $(this)
    
          modal.find('#id').val(iduser)
          modal.find('#qt').val(qt)
          modal.find('#name').val(name)
          modal.find('#compra').val(valorCompra)
          modal.find('#lucro').val(lucro)
});

$('#editarcardapio').on('show.bs.modal', function (event) {

          var button = $(event.relatedTarget)
          var idcard= button.data('cardid')
          var valorcard = button.data('cardvalor')
          var descricaocard = button.data('carddescricao');
          
        for(i = 0; i < descricaocard.length ; i++){
            console.log(descricaocard[i]);
        }
          
          var modal = $(this)      


          modal.find('#idcard').val(idcard)
          modal.find('#valor').val(valorcard)
          modal.find('#descricao').val(descricaocard)
         
});
    </script>

</html>
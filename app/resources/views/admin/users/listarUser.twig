{% extends 'layoutAdmin.twig' %}

{% block content %}


<div class="container">
<div class="row">
  <div class="col s12 m12 5" style="margin-top:50px;">

       <a href="/newuser" class="waves-effect waves-light">User

         <i class="material-icons left">add_box</i>
      </a>

<table  id='example' class='mdl-data-table  striped'>
  <thead class="thead btn-light">
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Nome</th>
      <th scope="col">Action</th>
    </tr>
</thead>


<tbody>
    {% for user in users %}
    <tr>
      <th scope="row">{{user.id}}</th>
      <td>{{user.nome }}</td>
      <td>




      <a href="/update/{{user.id}}" class="waves-effect waves-light green-text darken-1">User
        <i class="small material-icons green-text darken-1 left">face</i>
      </a>

         <a href="/UpdateUserEndeId?id={{user.id}}" class="waves-effect waves-light blue-text darken-1">Endereço
           <i class="small material-icons blue-text darken-1 left">search</i>

        </a>

         <a href="/deleteuser/{{user.id}}" class="waves-effect waves-light orange-text darken-1 left">Excluir
         <i class="small material-icons orange-text darken-1 left">restore_from_trash</i>

      </a>

      </td>
    </tr>
    {% endfor %}
  </tbody>

  </table>
</div>
{% endblock  %}
